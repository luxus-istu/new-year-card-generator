{
  "name": "new-year-card-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend && npm install --prefix shared",
    "build:shared": "npm run build --prefix shared",
    "dev": "npm run build:shared && concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix shared && npm run build --prefix backend && npm run build --prefix frontend",
    "start": "NODE_ENV=production node dist/server/server.js",
    "start:dev": "NODE_ENV=development node dist/server/server.js"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  },
  "dependencies": {
    "cors": "2.8.5",
    "express": "5.2.1",
    "nodemailer": "7.0.11"
  }
}
